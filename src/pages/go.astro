---
import BrandLayout from "../layouts/BrandLayout.astro";
import site from "../../data/site.json";

const title = `${site.mainKey} Weiterleitung`;
const description = `${site.mainKey} Weiterleitungsseite fuer den konfigurierten Anbieterlink.`;
---

<BrandLayout title={title} description={description} path="/go/" commercial={false}>
  <section class="hero" slot="hero">
    <div class="hero-inner">
      <div>
        <p class="eyebrow">Weiterleitung</p>
        <h1>Weiterleitung</h1>
        <p class="lead">Diese Route bleibt ein kontrollierter Zwischenpunkt. In Production kann Cloudflare `_redirects` den Pfad auf den freigegebenen Affiliate-Link leiten.</p>
      </div>
    </div>
  </section>
  <section class="content">
    <h2>Aktueller Status</h2>
    <p>Affiliate-Ziel: noch nicht freigegeben. Vor Production muss der echte Ziel-Link geprüft und verbunden werden.</p>
  </section>
</BrandLayout>
